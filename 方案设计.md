# 方案设计

## 一、功能需求分析

### 1.1 系统总体功能

本系统是一个面向工业产品表面缺陷检测的 B/S 架构 Web 应用，核心功能是用户通过浏览器上传工业产品表面图片，系统自动调用 AI 模型进行缺陷检测，并将检测结果（缺陷类型、位置、置信度）可视化展示给用户。

### 1.2 功能模块划分

#### 模块一：用户管理模块
- 用户注册与登录
- 用户权限管理（普通用户 / 管理员）
- 个人检测历史记录查询

#### 模块二：图像上传与预处理模块
- 支持 JPG、PNG、BMP 格式图片上传
- 单张图片大小限制 ≤ 10MB
- 自动进行图像预处理（尺寸归一化、格式转换）
- 支持批量上传（最多 10 张）

#### 模块三：缺陷检测模块
- 缺陷分类：判断图片中是否存在缺陷及缺陷类型（6 类）
- 缺陷定位：在图片上标注缺陷位置（边界框）
- 置信度显示：每个检测结果附带置信度分数
- 检测模式切换：快速模式（仅分类）/ 精确模式（分类+定位）

#### 模块四：结果展示与导出模块
- 检测结果图片可视化展示（标注框叠加）
- 检测详情列表（缺陷类型、置信度、坐标、面积）
- 检测报告导出（PDF / Excel 格式）
- 历史检测记录查询与筛选

#### 模块五：模型管理模块（管理员）
- 模型版本切换
- 模型性能指标查看（准确率、mAP、推理速度）
- 检测阈值配置（置信度阈值、NMS 阈值）

#### 模块六：系统管理模块
- 检测记录统计与可视化（缺陷类型分布、每日检测量）
- 系统运行状态监控
- 数据备份与恢复

### 1.3 非功能需求

| 需求类型 | 具体要求 |
|---------|---------|
| 性能 | 单张图片检测响应时间 ≤ 3 秒（CPU 环境） |
| 准确率 | 缺陷分类准确率 ≥ 90%，检测 mAP@0.5 ≥ 0.80 |
| 并发 | 支持至少 5 个用户同时在线检测 |
| 可用性 | 界面简洁友好，操作流程 ≤ 3 步完成检测 |
| 可扩展性 | 支持新增缺陷类型和替换检测模型 |
| 兼容性 | 支持 Chrome、Edge、Firefox 主流浏览器 |

---

## 二、方案论证

### 2.1 技术路线论证

#### 方案 A：纯前端推理（TensorFlow.js）
- **优点**：无需后端服务器，隐私性好，部署简单
- **缺点**：模型大小受限，推理速度慢，浏览器兼容性问题，不支持批量处理
- **结论**：不适用，工业检测模型较大，前端推理性能不足

#### 方案 B：后端推理 + RESTful API（本方案）
- **优点**：模型部署灵活，支持 GPU 加速，前后端解耦，可扩展
- **缺点**：需要维护后端服务，有网络传输开销
- **结论**：适用，是工业 AI 应用的主流架构

#### 方案 C：调用第三方云 API
- **优点**：无需自己训练模型，开发速度快
- **缺点**：依赖外部服务，数据隐私问题，定制化能力差，成本高
- **结论**：不适用，课程设计要求自主实现算法模块

**最终选择：方案 B — 后端推理 + RESTful API 的 B/S 架构**

### 2.2 算法方案论证

#### 分类模型选型

| 模型 | 参数量 | Top-1 准确率 | 推理速度 | 选择理由 |
|------|--------|-------------|---------|---------|
| ResNet50 | 25.6M | 76.1% | 中 | 经典骨干网络，迁移学习生态完善，工业检测常用 |
| EfficientNet-B0 | 5.3M | 77.1% | 快 | 轻量化，准确率高，但调参复杂度较高 |
| VGG16 | 138M | 71.5% | 慢 | 参数量大，性能不如 ResNet |

**选择：ResNet50** — 工业缺陷检测领域最常用的骨干网络，文档丰富，迁移学习效果稳定。

#### 检测模型选型

| 模型 | 输入尺寸 | mAP@0.5 | 推理速度 | 选择理由 |
|------|---------|---------|---------|---------|
| YOLOv8n | 640 | 37.3% | 极快 | 轻量化版本，适合实时检测 |
| YOLOv8s | 640 | 44.9% | 快 | 速度与精度平衡，适合本项目 |
| Faster R-CNN | 600 | 42.0% | 慢 | 两阶段检测，精度高但速度慢 |

**选择：YOLOv8s** — 单阶段检测，速度快，精度满足需求，工业应用广泛，开源社区活跃。

### 2.3 数据集方案论证

| 数据集 | 产品类型 | 缺陷类别 | 数据量 | 标注格式 | 选择理由 |
|--------|---------|---------|--------|---------|---------|
| NEU-DET | 热轧钢带 | 6类 | 1800张 | XML(PASCAL VOC) | 经典工业缺陷数据集，类别均衡，适合教学 |
| MVTec AD | 多品类 | 15类 | 5000+张 | 像素级掩码 | 异常检测标准数据集，但无边界框标注 |
| Magnetic Tile | 磁性瓷砖 | 5类 | 1500张 | XML | 数据量较小，类别不均衡 |

**选择：NEU-DET** — 6 类缺陷（裂纹、夹杂、斑块、麻点、氧化皮、划痕），每类 300 张，数据均衡，有边界框标注，适合分类和检测双任务。

---

## 三、方案规划

### 3.1 技术栈

#### 前端技术栈
| 技术 | 版本 | 用途 |
|------|------|------|
| HTML5 + CSS3 | - | 页面结构与样式 |
| JavaScript (ES6+) | - | 前端交互逻辑 |
| Vue 3 | 3.x | 前端框架（可选，简化版用原生 JS） |
| ECharts | 5.x | 数据统计图表可视化 |
| Axios | 1.x | HTTP 请求库 |
| Bootstrap 5 | 5.x | UI 组件库（简化开发） |

#### 后端技术栈
| 技术 | 版本 | 用途 |
|------|------|------|
| Python | 3.10 | 后端开发语言 |
| Flask | 2.x | Web 框架（轻量，适合课程设计） |
| PyTorch | 2.x | 深度学习框架 |
| torchvision | 0.x | 计算机视觉工具库 |
| OpenCV | 4.x | 图像处理 |
| NumPy | 1.x | 数值计算 |
| Pillow | 10.x | 图片处理 |
| ultralytics | 8.x | YOLOv8 实现库 |

#### 数据库
| 技术 | 版本 | 用途 |
|------|------|------|
| SQLite | 3.x | 轻量关系型数据库（无需额外安装，适合课程设计） |
| SQLAlchemy | 2.x | ORM 框架 |

#### 开发与部署工具
| 工具 | 用途 |
|------|------|
| Git | 版本控制 |
| GitHub | 代码托管 |
| pip | Python 包管理 |
| Postman | API 测试 |

### 3.2 需配置的外部资源

1. **飞桨 AI Studio（可选）**：https://aistudio.baidu.com/ — 提供免费 GPU 算力用于模型训练
2. **NEU-DET 数据集**：公开下载，用于模型训练和测试
3. **预训练模型权重**：
   - ResNet50 ImageNet 预训练权重（torchvision 自动下载）
   - YOLOv8s COCO 预训练权重（ultralytics 自动下载）
4. **Python 依赖包**：通过 requirements.txt 统一管理

### 3.3 数据集来源

- **主数据集**：NEU-DET 热轧钢带表面缺陷数据集
  - 来源：东北大学工业自动化实验室公开数据集
  - 下载地址：https://www.kaggle.com/datasets/kaustubhdikshit/neu-surface-defect-database
  - 数据规模：1800 张灰度图像，6 类缺陷，每类 300 张
  - 标注格式：PASCAL VOC XML 格式边界框标注

### 3.4 前端 UI 设计

#### 页面结构
1. **登录/注册页**：用户认证入口
2. **首页（检测页）**：
   - 左侧：图片上传区域（拖拽或点击上传）
   - 右侧：检测结果展示（标注图片 + 详情列表）
   - 底部：检测按钮、模式切换、历史记录快捷入口
3. **历史记录页**：
   - 检测记录列表（时间、文件名、缺陷数量、操作）
   - 筛选功能（按日期、缺陷类型）
   - 详情查看与报告导出
4. **统计仪表盘页**（管理员）：
   - 检测量趋势图
   - 缺陷类型分布饼图
   - 模型性能指标卡片
5. **模型管理页**（管理员）：
   - 模型版本列表与切换
   - 检测阈值配置

#### 设计风格
- 工业风配色：深蓝（#1a3a5c）+ 橙色（#ff6b35）点缀
- 简洁卡片式布局
- 响应式设计，适配不同屏幕尺寸

### 3.5 后端算法设计

#### API 接口设计

| 接口 | 方法 | 路径 | 功能 |
|------|------|------|------|
| 用户注册 | POST | /api/auth/register | 新用户注册 |
| 用户登录 | POST | /api/auth/login | 用户登录，返回 token |
| 图片上传检测 | POST | /api/detect/upload | 上传图片并执行检测 |
| 批量检测 | POST | /api/detect/batch | 批量上传检测 |
| 检测记录列表 | GET | /api/records | 获取当前用户检测记录 |
| 检测记录详情 | GET | /api/records/{id} | 获取单条记录详情 |
| 删除记录 | DELETE | /api/records/{id} | 删除检测记录 |
| 导出报告 | GET | /api/records/{id}/export | 导出检测报告 |
| 统计数据 | GET | /api/stats | 获取统计仪表盘数据 |
| 模型列表 | GET | /api/models | 获取可用模型列表 |
| 切换模型 | PUT | /api/models/active | 切换当前使用的模型 |
| 更新阈值 | PUT | /api/models/threshold | 更新检测阈值 |

#### 算法模块架构

```
算法模块
├── preprocess/          # 图像预处理
│   ├── resize.py        # 尺寸归一化
│   ├── augment.py       # 数据增强（训练用）
│   └── normalize.py     # 图像归一化
├── models/              # 模型定义
│   ├── classifier.py    # ResNet50 分类模型
│   └── detector.py      # YOLOv8 检测模型封装
├── inference/           # 推理引擎
│   ├── classify.py      # 分类推理
│   ├── detect.py        # 检测推理
│   └── pipeline.py      # 检测流水线（分类+检测）
├── postprocess/         # 后处理
│   ├── nms.py           # 非极大值抑制
│   ├── visualize.py     # 结果可视化（标注框绘制）
│   └── report.py        # 检测报告生成
└── eval/                # 模型评估
    ├── metrics.py       # 评估指标计算
    └── confusion.py     # 混淆矩阵生成
```

#### 检测流水线

1. 接收上传图片 → 2. 图像预处理（resize、normalize）→ 3. 分类模型判断是否有缺陷 → 4. 如有缺陷，检测模型定位缺陷位置 → 5. 后处理（NMS、阈值过滤）→ 6. 结果可视化（绘制标注框）→ 7. 保存记录到数据库 → 8. 返回结果 JSON + 标注图片

### 3.6 数据库设计

#### 用户表 (users)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键，自增 |
| username | VARCHAR(50) | 用户名，唯一 |
| password_hash | VARCHAR(255) | 密码哈希 |
| role | VARCHAR(20) | 角色（user/admin） |
| created_at | DATETIME | 创建时间 |

#### 检测记录表 (detection_records)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键，自增 |
| user_id | INTEGER | 外键，关联用户 |
| filename | VARCHAR(255) | 原始文件名 |
| image_path | VARCHAR(500) | 原图存储路径 |
| result_image_path | VARCHAR(500) | 结果图标注路径 |
| defect_count | INTEGER | 检测到的缺陷数量 |
| defect_types | VARCHAR(255) | 缺陷类型列表（JSON） |
| confidence_avg | FLOAT | 平均置信度 |
| model_version | VARCHAR(50) | 使用的模型版本 |
| detection_mode | VARCHAR(20) | 检测模式（fast/precise） |
| created_at | DATETIME | 检测时间 |

#### 缺陷详情表 (defect_details)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键，自增 |
| record_id | INTEGER | 外键，关联检测记录 |
| defect_type | VARCHAR(50) | 缺陷类型 |
| confidence | FLOAT | 置信度 |
| bbox_x | INTEGER | 边界框左上角 x |
| bbox_y | INTEGER | 边界框左上角 y |
| bbox_w | INTEGER | 边界框宽度 |
| bbox_h | INTEGER | 边界框高度 |
| area | INTEGER | 缺陷面积（像素） |

---

## 四、技术路线

### 4.1 总体技术路线图

```
需求分析 → 数据准备 → 模型开发 → 系统开发 → 集成测试 → 成果交付
    ↓          ↓          ↓          ↓          ↓          ↓
  功能定义   数据收集   分类模型   前端UI    接口联调    设计报告
  方案论证   数据清洗   检测模型   后端API   自动化测试  演示视频
  技术选型   数据增强   模型训练   数据库    性能优化    答辩PPT
             标注转换   模型评估   算法集成   缺陷修复
```

### 4.2 算法技术路线

1. **数据准备阶段**
   - 下载 NEU-DET 数据集
   - XML 标注转换为 YOLO TXT 格式和 COCO JSON 格式
   - 按 7:2:1 划分训练集、验证集、测试集
   - 数据增强：随机翻转、旋转、亮度扰动、Mosaic

2. **分类模型开发**
   - 基于 torchvision 加载 ResNet50 预训练权重
   - 替换最后一层全连接层为 6 分类输出
   - 冻结骨干网络前 10 轮训练，后 20 轮微调
   - 使用交叉熵损失 + Adam 优化器
   - 评估：准确率、混淆矩阵、分类报告

3. **检测模型开发**
   - 使用 ultralytics 库加载 YOLOv8s 预训练权重
   - 配置 NEU-DET 数据集 yaml 文件
   - 训练 100 轮，batch=16，img=640
   - 评估：mAP@0.5、mAP@0.5:0.95、PR 曲线

4. **模型优化**
   - 错误样本分析，针对性数据增强
   - 超参数调优（学习率、权重衰减）
   - 模型导出为 ONNX 格式，优化推理速度

### 4.3 系统开发技术路线

1. **后端开发**：Flask 框架搭建 → 数据库模型设计 → API 接口开发 → 算法模块集成 → 单元测试
2. **前端开发**：页面结构搭建 → 样式实现 → 交互逻辑 → API 对接 → 响应式适配
3. **集成调试**：前后端联调 → 端到端测试 → 性能优化 → Bug 修复

---

## 五、计划安排

### 5.1 总体进度（共 10 天）

| 阶段 | 时间 | 主要任务 | 交付物 |
|------|------|---------|--------|
| 第一阶段 | D1-D2 | 工具配置、vibe coding 学习、选题调研 | 学习笔记.md、GitHub 仓库 |
| 第二阶段 | D3-D5 | 选题与方案设计 | 选题说明.md、方案设计.md |
| 第三阶段 | D6 | 数据资源整理 | /data 目录、预处理脚本 |
| 第四阶段 | D7-D8 | 详细开发（算法+系统） | 完整可运行系统代码 |
| 第五阶段 | D9 | 集成调试、报告撰写 | 设计报告、演示视频 |
| 第六阶段 | D10 | 答辩 | 答辩 PPT |

### 5.2 详细周计划

#### D1-D2（第一阶段：学习与调研）
- D1：GitHub 账号注册、仓库创建、Git 学习、AI 工具试用
- D2：工业缺陷检测领域调研、数据集调研、选题确定、学习笔记撰写

#### D3-D5（第二阶段：方案设计）
- D3：选题说明撰写、技术方向映射
- D4：功能需求分析、方案论证、技术栈选型
- D5：系统架构设计、数据库设计、API 设计、方案设计文档撰写

#### D6（第三阶段：数据准备）
- 下载 NEU-DET 数据集
- 编写数据预处理脚本（标注格式转换、数据集划分）
- 数据增强实现与验证
- 更新 README 和数据说明

#### D7-D8（第四阶段：详细开发）
- D7 上午：后端框架搭建、数据库模型、用户认证 API
- D7 下午：算法模块集成（分类+检测推理流水线）
- D8 上午：检测 API、记录管理 API、统计 API
- D8 下午：前端页面开发（检测页、历史页、登录页）

#### D9（第五阶段：集成调试）
- 上午：前后端联调、端到端测试、Bug 修复
- 下午：设计报告撰写、演示视频录制
- 晚上：答辩 PPT 制作

#### D10（第六阶段：答辩）
- 上午：系统最终测试、答辩准备
- 下午：答辩

### 5.3 里程碑与验收标准

| 里程碑 | 时间 | 验收标准 |
|--------|------|---------|
| M1：学习阶段完成 | D2 结束 | 学习笔记.md 提交，仓库可访问 |
| M2：方案设计完成 | D5 结束 | 选题说明.md、方案设计.md 提交，方案通过审核 |
| M3：数据准备完成 | D6 结束 | /data 目录完整，预处理脚本可运行 |
| M4：系统开发完成 | D8 结束 | 系统可运行，核心功能可用 |
| M5：集成调试完成 | D9 结束 | 自动化测试通过，报告和视频完成 |
| M6：答辩完成 | D10 结束 | 答辩通过 |

---

## 六、参考文献

### 6.1 教材与专著

[1] 张智海等. 制造智能技术基础[M]. 北京: 清华大学出版社, 2022.

[2] 杨杰. 人工智能基础[M]. 北京: 机械工业出版社, 2025.

[3] 周志华. 机器学习[M]. 北京: 清华大学出版社, 2016.

[4] 何恺明, 张祥雨, 任少卿, 等. Deep Residual Learning for Image Recognition[C]//CVPR, 2016.

### 6.2 学术论文

[5] He K, Zhang X, Ren S, et al. Deep Residual Learning for Image Recognition[C]//Proceedings of the IEEE Conference on Computer Vision and Pattern Recognition. 2016: 770-778.

[6] Redmon J, Farhadi A. YOLOv3: An Incremental Improvement[J]. arXiv preprint arXiv:1804.02767, 2018.

[7] Jocher G, Chaurasia A, Qiu J. Ultralytics YOLOv8[EB/OL]. https://github.com/ultralytics/ultralytics, 2023.

[8] He Y, Song K, Meng Q, et al. An End-to-end Steel Surface Defect Detection Approach via Fusing Multiple Hierarchical Features[J]. IEEE Transactions on Instrumentation and Measurement, 2020, 69(4): 1493-1504.

[9] Tabernik D, Šela S, Skvarč J, et al. Segmentation-based deep-learning approach for surface-defect detection[J]. Journal of Intelligent Manufacturing, 2020, 31(3): 759-776.

[10] Bergmann P, Fauser M, Sattlegger D, et al. MVTec AD-A Comprehensive Real-World Dataset for Unsupervised Anomaly Detection[C]//CVPR, 2019: 9592-9600.

### 6.3 在线资源

[11] PyTorch 官方文档. https://pytorch.org/docs/

[12] Flask 官方文档. https://flask.palletsprojects.com/

[13] Ultralytics YOLOv8 文档. https://docs.ultralytics.com/

[14] NEU-DET 数据集. https://www.kaggle.com/datasets/kaustubhdikshit/neu-surface-defect-database

[15] 飞桨 AI Studio. https://aistudio.baidu.com/

[16] ModelScope 魔搭社区. https://modelscope.cn/

---

## 七、风险评估与应对

| 风险 | 可能性 | 影响 | 应对措施 |
|------|--------|------|---------|
| 模型训练准确率不达标 | 中 | 高 | 使用迁移学习、增加数据增强、调整网络结构 |
| 数据集下载失败 | 低 | 高 | 准备备用数据集（Magnetic Tile），或使用数据生成 |
| 开发时间不足 | 中 | 中 | 优先实现核心功能（上传+检测+展示），裁剪非必要功能 |
| 前后端联调问题 | 中 | 中 | 提前定义 API 接口规范，使用 Mock 数据并行开发 |
| GPU 算力不足 | 中 | 中 | 使用飞桨 AI Studio 免费 GPU，或减小模型规模 |
