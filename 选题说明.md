# 选题说明

## 一、拟定题目

**基于深度学习的工业产品表面缺陷智能检测系统**

英文题目：Intelligent Surface Defect Detection System for Industrial Products Based on Deep Learning

---

## 二、完成目标

### 2.1 总体目标

开发一个面向制造/工业场景的 **B/S + C/S 双架构** 智能检测系统，实现工业产品表面缺陷的自动化识别与定位，替代传统人工目视检测，提升质量检测效率和准确性。系统同时提供 Web 浏览器端（B/S）和桌面客户端（C/S）两种交互界面，满足不同使用场景需求。

### 2.2 具体目标

1. **数据层目标**
   - 收集并整理至少 1 个公开工业缺陷数据集（NEU-DET 热轧钢带表面缺陷数据集）
   - 完成数据清洗、标注格式转换、数据增强等预处理工作
   - 建立训练集、验证集、测试集的标准划分

2. **算法层目标**
   - 实现基于卷积神经网络（CNN）的缺陷分类模型，分类准确率 ≥ 90%
   - 实现基于 YOLOv8 的缺陷目标检测模型，mAP@0.5 ≥ 0.80
   - 对比不同模型（ResNet、YOLOv8）的性能，给出选型依据

3. **系统层目标**
   - 实现完整的 **B/S + C/S 双架构** 应用
   - **B/S 端（Web）**：支持图片上传、检测结果可视化展示、历史记录查询、统计仪表盘、模型管理
   - **C/S 端（桌面客户端）**：基于 Tkinter 的桌面应用，支持登录、图片检测、历史记录查看、统计信息展示，调用同一套后端 RESTful API
   - 后端：提供 RESTful API，承载图像预处理、模型推理、结果返回逻辑
   - 数据库：存储检测记录、用户信息、模型配置
   - 系统可通过自动化测试，达到可现场演示状态

4. **工程层目标**
   - 全程使用 vibe coding 方法开发，保留完整 prompt 会话日志
   - 使用 Git 进行版本控制，小步提交并撰写有意义的提交说明
   - 编写完整的 README 文档和设计报告

---

## 三、涉及的技术方向

本选题涉及《制造智能技术基础》课程的以下技术方向（共 4 个，满足至少 3 个的要求）：

### 3.1 技术方向一：深度学习与卷积神经网络

**对应课程章节：深度学习与神经网络基础**

**在系统中的实际作用：**
- 使用卷积神经网络（CNN）作为核心算法，提取工业产品表面图像的缺陷特征
- 采用 ResNet50 作为分类骨干网络，实现 6 类缺陷的自动分类
- 采用 YOLOv8 作为目标检测网络，实现缺陷的定位和边界框回归
- 通过迁移学习（使用 ImageNet 预训练权重）加速模型收敛，提升小数据集上的性能

**关键技术点：**
- 卷积层、池化层、全连接层的网络结构设计
- 反向传播与梯度下降优化算法（Adam、SGD）
- 批归一化（BatchNorm）、Dropout 等正则化技术
- 损失函数设计（交叉熵损失、CIoU 损失）

### 3.2 技术方向二：计算机视觉与图像处理

**对应课程章节：机器视觉与图像处理**

**在系统中的实际作用：**
- 图像预处理：灰度化、去噪、对比度增强、尺寸归一化
- 数据增强：随机裁剪、旋转、翻转、亮度扰动、Mosaic 增强，扩充训练样本
- 特征可视化：通过 Grad-CAM 可视化模型关注区域，验证模型是否学习到正确的缺陷特征
- 检测结果后处理：非极大值抑制（NMS）、置信度阈值过滤、缺陷面积计算

**关键技术点：**
- 空间域与频率域图像增强方法
- 图像几何变换与插值算法
- 颜色空间转换（RGB、HSV、灰度）
- 形态学操作（腐蚀、膨胀、开运算、闭运算）

### 3.3 技术方向三：机器学习与模式识别

**对应课程章节：机器学习与模式识别基础**

**在系统中的实际作用：**
- 监督学习范式：使用标注数据训练分类和检测模型
- 模型评估：使用准确率、精确率、召回率、F1-score、混淆矩阵、mAP 等指标全面评估模型性能
- 超参数优化：通过网格搜索和经验调优确定学习率、批量大小、训练轮数等超参数
- 误差分析：分析模型混淆类别和误检样本，指导数据增强和模型改进方向

**关键技术点：**
- 训练集/验证集/测试集划分与交叉验证
- 过拟合与欠拟合的识别与应对
- 混淆矩阵与 ROC 曲线分析
- 模型选择与集成学习思想

### 3.4 技术方向四：智能制造系统与架构

**对应课程章节：智能制造系统与信息集成**

**在系统中的实际作用：**
- 设计 **B/S + C/S 双架构** 的智能检测系统，明确前端（Web端 + 桌面客户端）、后端、数据库、算法模块的分层架构
- B/S 端：基于 Flask + Bootstrap + ECharts 的 Web 应用，浏览器即开即用，适合多用户访问和管理端场景
- C/S 端：基于 Tkinter + requests 的桌面客户端，调用同一套后端 API，适合产线单机操作和离线场景
- 设计 RESTful API 接口规范，实现前后端解耦，B/S 和 C/S 端共享同一套后端服务
- 设计检测流程与生产管理系统的集成方案（检测结果可导出为报表，支持质量追溯）
- 考虑系统的可扩展性，支持新增缺陷类型和模型版本管理

**关键技术点：**
- 制造系统的层级架构（设备层、控制层、执行层、计划层）
- 工业互联网与数据采集
- 质量检测在制造流程中的定位与价值流分析
- 系统模块化设计与接口规范

---

## 四、技术方向映射表

| 序号 | 课程技术方向 | 对应章节 | 在系统中的模块 | 具体应用 |
|------|-------------|---------|--------------|---------|
| 1 | 深度学习与卷积神经网络 | 深度学习与神经网络 | 算法模块 | ResNet50 分类、YOLOv8 检测 |
| 2 | 计算机视觉与图像处理 | 机器视觉与图像处理 | 算法模块 + 后端 | 图像预处理、数据增强、结果可视化 |
| 3 | 机器学习与模式识别 | 机器学习基础 | 算法模块 | 模型训练、评估、超参数优化 |
| 4 | 智能制造系统与架构 | 智能制造系统 | 系统整体 | B/S + C/S 双架构设计、RESTful API 接口、桌面客户端、流程集成 |

---

## 五、选题创新点与意义

### 5.1 应用意义

- 解决传统人工检测效率低、一致性差的问题，实现质量检测的自动化和智能化
- 检测结果数字化存储，支持质量追溯和数据分析，为工艺改进提供数据支撑
- 系统架构可扩展，可迁移到其他工业检测场景（如 PCB 缺陷、铸件缺陷等）

### 5.2 技术特点

- 采用分类 + 检测双模型架构，兼顾检测速度和定位精度
- 使用迁移学习降低数据需求，适合工业场景缺陷样本稀缺的特点
- 完整的 B/S 架构实现，从算法到系统全链路打通，具备实际演示价值

---

## 六、选题确认

本选题为自主选题，未与已提交选题重复。选题场景为制造/工业环节（热轧钢带表面质量检测），具有真实业务意义，不涉及日常生活场景。
